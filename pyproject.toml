[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "transfer-learning-fwi"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "pandas",
    "tables",
    "matplotlib",
    "torch",
    "PyYAML",
    "transformers",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["src*"]
